/**
 * Contact email shown in the UI and used in mailto links.
 * Set NEXT_PUBLIC_CONTACT_EMAIL in .env to override.
 */
export const CONTACT_EMAIL =
  process.env.NEXT_PUBLIC_CONTACT_EMAIL || 'hello@queermusicfest.com'
